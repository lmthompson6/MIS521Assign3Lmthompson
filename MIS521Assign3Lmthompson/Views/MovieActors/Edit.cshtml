@model MIS521Assign3Lmthompson.Models.MovieActor

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Casting</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Actor.Name" class="control-label"></label>
                <select asp-for="Actor.Name" class="form-control" asp-items="ViewBag.ActorID"></select>
                <span asp-validation-for="Actor.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Movie.Title" class="control-label"></label>
                <select asp-for="Movie.Title" class="form-control" asp-items="ViewBag.MovieID"></select>
                <span asp-validation-for="Movie.Title" class="text-danger"></span>
            </div>
            <p></p>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
